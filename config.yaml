baseURL: https://www.hienph.dev
title: HienPH's Notes
theme: book
defaultContentLanguage: vi

# Book configuration
disablePathToLower: true
enableGitInfo: true

# comments
disqusShortname: hien-ph

# Needed for mermaid/katex shortcodes
markup:
  goldmark:
    renderer:
      unsafe: true
  tableOfContents:
    startLevel: 1
  # your our own highlight
  highlight:
    codeFences: false

languages:
  vi:
    languageName: Vietnamese
    contentDir: content
    weight: 1
  en:
    languageName: English
    contentDir: content
    weight: 2

menu:
  before:
    - name: "My Profile"
      url: "https://my.hienph.dev"
      weight: 1
  after:
    - name: Github
      pre: <i data-feather="github" class="feather-menu" width=18 height=18></i>
      url: "https://github.com/hienduyph"
      weight: 10
    - name: "Linkedin"
      pre: <i data-feather="linkedin" class="feather-menu" width=18 height=18></i>
      url: "https://linkedin.com/in/hienduyph"
      weight: 20
    - name: "Facebook"
      pre: <i data-feather="facebook" class="feather-menu" width=18 height=18></i>
      url: "https://facebook.com/hienduyph"
      weight: 20
    - name: "Twitter"
      pre: <i data-feather="twitter" class="feather-menu" width=18 height=18></i>
      url: "https://twitter.com/hienduyph"
      weight: 20
    - name: "Source"
      pre: <i data-feather="code" class="feather-menu" width=18 height=18></i>
      url: "https://github.com/hienduyph/hienph.dev"
      weight: 30

params:
  # (Optional, default true) Controls table of contents visibility on right side of pages.
  # Start and end levels can be controlled with markup.tableOfContents setting.
  # You can also specify this parameter per page in front matter.
  BookToC: true

  # (Optional, default none) Set the path to a logo for the book. If the logo is
  # /static/logo.png then the path would be logo.png
  BookLogo: /logo.png

  # (Optional, default none) Set leaf bundle to render as side menu
  # When not specified file structure and weights will be used
  BookMenuBundle: /menu

  # (Optional, default docs) Specify section of content to render as menu
  # You can also set value to '*' to render all sections to menu
  BookSection: journey

  # Set source repository location.
  # Used for 'Last Modified' and 'Edit this page' links.
  # BookRepo: https://github.com/hienduyph

  # Enable "Edit this page" links for 'doc' page type.
  # Disabled by default. Uncomment to enable. Requires 'BookRepo' param.
  # Path must point to 'content' directory of repo.
  # BookEditPath: edit/master/exampleSite/content

  # Configure the date format used on the pages
  # - In git information
  # - In blog posts
  BookDateFormat: "Jan 2, 2006"

  # (Optional, default true) Enables search function with flexsearch,
  # Index is built on fly, therefore it might slowdown your website.
  # Configuration for indexing can be adjusted in i18n folder per language.
  BookSearch: false

  # (Optional, default true) Enables comments template on pages
  # By default partals/docs/comments.html includes Disqus template
  # See https://gohugo.io/content-management/comments/#configure-disqus
  # Can be overwritten by same param in page frontmatter
  BookComments: true

  # /!\ This is an experimental feature, might be removed or changed at any time
  # (Optional, experimental, default false) Enables portable links and link checks in markdown pages.
  # Portable links meant to work with text editors and let you write markdown without {{< relref >}} shortcode
  # Theme will print warning if page referenced in markdown does not exists.
  BookPortableLinks: true

  Description: Share the ideas and my experiments
